  <nav class="navbar navbar-expand-lg navbar-light bg-white ">
        <div class="container">
          <i class="fa fa-instagram fa-2x pr-2"> </i>
          <%= link_to "Instagram", dashboard_path, class:"navbar-brand text-decoration-none "%>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav ">
             <!-- If account  signed in then show nav bar-->
            <% if account_signed_in? %>
                <li class="nav-item ">
                  <form class="form-inline" method="get" action="<%= search_index_path %>">
                    <div class="input-group">
                      <input type="text" class="form-control" name="query" placeholder="Search users...">
                      <div class="input-group-append">
                        <button class="btn btn-outline-secondary" type="submit"><i class="fa fa-search"></i></button>
                      </div>
                    </div>
                  </form>
                </li>
                <%# <li class="nav-item">
                  <%= link_to "Dashboard", dashboard_path, class:"nav-link"%>
                <%# </li> %>
                <li class="nav-item">
                  <%= link_to "Post", new_post_path, class:"nav-link"%>
                </li>
                <li class="nav-item">
                  <%= link_to "My Profile", profile_path(current_account.username), class:"nav-link"%>
                </li>
                  <li class="nav-item">
                  <%= link_to "Stories", stories_path , class:"nav-link"%>
                </li>
                <li class="nav-item">
                  <%= button_to "Sign Out", destroy_account_session_path, method: :delete ,class:"btn btn-link text-decoration-none text-muted"%>
                </li>
            <%else%>
              <!-- If account not signed in then show else part-->
            <%end%>
            </ul>
          </div>
        </div>
      </nav>
