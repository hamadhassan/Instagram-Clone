  <nav class="navbar navbar-expand-lg navbar-light bg-white ">
        <div class="container">
          <a class="navbar-brand" href="<% account_signed_in? ? dashboard_path : root_path%>">
          <i class="fa fa-instagram"> </i>
          Instagram Clone

          </a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
            <ul class="navbar-nav">
            <% if account_signed_in? %>
             <li class="nav-item">
                  <%= link_to "Dashboard", dashboard_path, class:"nav-link"%>
                </li>
                <li class="nav-item">
                  <%= link_to "New Post", new_post_path, class:"nav-link"%>
                </li>
                <li class="nav-item">
                  <%= link_to "My Profile", profile_path(current_account.username), class:"nav-link"%>
                </li>
                <li class="nav-item">
                  <%= button_to "Sign Out", destroy_account_session_path, method: :delete ,class:"btn btn-link text-decoration-none text-muted"%>
                </li>
            <%else%>
              <%= render "layouts/public_nav"%>
            <%end%>
            </ul>
          </div>
        </div>
      </nav>
